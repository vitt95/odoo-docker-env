"""Tests of the dictionary and the catalogue — no platform, no database, no clock.

Run with `python3 tools/pure/run.py`, and inside the Odoo suite through the bridge in
`nli_semantics/tests/test_pure_zone.py`.
"""
