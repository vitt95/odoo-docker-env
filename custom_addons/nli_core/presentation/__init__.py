"""The Presenter: native views, and never a result without its interpretation.

V4 and D64 are the same statement seen from two sides: the interpretation is
always visible above the result, without interaction. The architectural check of
`04` §6.4 verifies the Presenter receives state and result together — a
signature that makes the alternative unrepresentable rather than discouraged.

Filled in part 4.
"""

from . import presenter
